import { NotificationSection } from "./Notificatiom.styled";
export const Notification = () => {
    return (
        <NotificationSection>
            <p>There is no feedback</p>
        </NotificationSection>
    );
};

export default Notification;